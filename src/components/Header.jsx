import React from "react";

const Header = () => {
    return (
        <nav className="navbar navbar-dark bg-warning justify-content-center mb-1 ">
            <span className="navbar-brand mb-0 h1 ">Gallery App</span>
        </nav>
    );
};

export default Header;
